import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.neurosoft.pedi2.repartidor.repartidor', // Nombre de paquete que coincide con google-services.json
  appName: 'Pedi2-Repartidor', // Cambia 'TuApp' por el nombre que prefieras para tu aplicación
  webDir: 'www',
};

export default config;
